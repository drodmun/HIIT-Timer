const timerStyles = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center"
};

const valueStyles = {
  fontWeight: 400,
  fontSize: "4.75rem"
};

export default { timerStyles, valueStyles };
